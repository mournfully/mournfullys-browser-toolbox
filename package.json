{
  "name": "mournfully-extension",
  "version": "1.2.0",
  "description": "comprehensive tab management",
  "scripts": {
    "start": "npm install && npm run clean && parcel build src/manifest.json",
    "build": "npm run clean && parcel build src/manifest.json",
    "watch": "npm run clean && parcel watch src/manifest.json --no-hmr",
    "test": "npx jest --collect-coverage",
    "lint": "npx eslint . --ext .ts,.js",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "mournfully",
  "license": "MIT",
  "devDependencies": {
    "@parcel/config-webextension": "^2.8.2",
    "@types/chrome": "^0.0.206",
    "@types/clipboard": "^2.0.7",
    "@types/jest": "^29.2.4",
    "@types/webextension-polyfill": "^0.9.2",
    "@typescript-eslint/eslint-plugin": "^5.47.1",
    "@typescript-eslint/parser": "^5.47.1",
    "eslint": "^8.30.0",
    "parcel": "^2.8.2",
    "typescript": "^4.9.4",
    "webextension-polyfill": "^0.10.0"
  },
  "dependencies": {
    "clipboard": "^2.0.11",
    "rimraf": "^3.0.2",
    "ts-debounce": "^4.0.0"
  }
}
